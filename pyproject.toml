[project]
name = "signal-chance-lab"
version = "1.0.0"
requires-python = ">=3.11,<3.13"
dependencies = [
    "Django==4.1.7",
    "python-dotenv==1.0.0",
]

[tool.vercel.scripts]
build = "python build.py"
